{{#each notes as |note|}}

  <!-- {{> partial}} -->

 <!-- Developers, why are we using data-id and not just id? -->
  <ul<data-id="{{note.id}}">
    <li> Comment: {{comment.title}}</li>
    <li>Time: {{time.description}}</li>
    <li><button class='remove'> remove</button></li>
  </ul>
